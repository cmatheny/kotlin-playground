version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - printenv
      - ls -l gradle
      - ./gradlew test
  build:
    commands:
      - ./gradlew shadowJar
artifacts:
  files:
    - build/libs/**/*
